<div class=" container border p-3 mb-4 ">
</br>
  <%= simple_form_for [@weektime, @worktime] do |f| %>
    <%= f.error_notification %>
    <%#= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

    
    <div>
      <%#= f.input :daytime,  wrapper: :horizontal_collection_inline, collection: Worktime.daytimes.keys  %>
      <%= f.input :daytime, as: :radio_buttons, collection:Worktime.daytimes.keys , wrapper: :horizontal_collection_inline %>

    </div>
    <div class="form-inputs">
      <%= f.input :gotime, as: :time %>
      <%= f.input :endtime, as: :time %>
     
    </div>
  </br>
    <div class="text-center">
      <%= f.button :submit %>
    </div>
  </br>
<% end %>

</div>