 </br>
<div class="border p-3 mb-0 container">
  <h2 class="text-center" > <strong> Semaine : </strong><%= @weektime.dateweek.cweek %> | <strong> Date : </strong><%= @weektime.dateweek.strftime("%Y-%m")    %></h2>
  <hr>
  <div class="container">
       <table class="table table-sm">
        <thead class="thead-light">
          <tr>
            <th><strong> Utilisateur </strong></th>
            <th><strong> Semaine </strong></th>
            <th><strong>nombre d'heure</strong></th>
            <th><strong>Total heure</strong></th>
            <th><strong>Validation</strong></th>
          </tr>
        </thead>
    <%if @worktime.empty? %> 
            <tr>
              <td> </td>
              <td> </td>
              <td> </td>
              <td> </td>
            </tr>
    <% else %>   
      <tbody>   
          <% @worktime.each do |work| %>
            <tr>
              <td> # </td>
              <td><%= work.gotime.to_fs(:time) %></td>
              <td><%= work.endtime.to_fs(:time) %></td>
              <td><%= Time.at(work.accord).utc.strftime("%H:%M") %></td>
            </tr>
          <% end %>
      </tbody>
    <%end%>  
        <thead class="thead-light">
          <tr>
            <th> </th>
            <th> </th>
            <th> </th>
            <th>Heures Total:<%= Time.at(@weektime.alltime).utc.strftime("%H:%M") %> </th>
            <th> </th>
          </tr>
        </thead>
      </table>
  </div>
  <Hr>
  <p>
    <strong>Accord:</strong>
    <%= @weektime.accord %>
  </p>
  <hr>
  <div class="container">
    <div class="text-center">
      <%= button_to "new worktime", new_weektime_worktime_path(@weektime), method: :get %>
    </div>
    <div class="pull-right row">
      <%= button_to "Modifier", edit_weektime_path(@weektime) %>   
      <%= button_to "Supprimer", @weektime, method: :delete %>
    </div>
  </div>
</div>

</br>
<div class="container">
<%= button_to "Dashbord", root_path, method: :get %>
</div>
